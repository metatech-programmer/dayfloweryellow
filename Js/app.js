const sectionButtons = document.querySelector(".buttons")

setTimeout(() => {
  sectionButtons.setAttribute("style", "display: flex; flex-direction: column; animation: aparicion 2s ease-in-out")
  
}, 8000);